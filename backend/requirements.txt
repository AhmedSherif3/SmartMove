# Install only what's needed for the Render deployment.
# development.txt and testing.txt are excluded to save memory
# (pytest, black, flake8, ipython, coverage aren't needed on a live server).
-r requirements/base.txt
-r requirements/render.txt
-r requirements/production.txt
